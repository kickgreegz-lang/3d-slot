/**
 * SWAMP FUNK scene events on top of the engine's CoreGameEvents (game/events.ts).
 * None: multiplier spots are an engine feature (spots:update / spots:reset).
 */
export type GameSceneEvents = Record<never, never>;
