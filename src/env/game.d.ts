/**
 * Build-time constants injected by vite.config.ts `define`.
 * `__GAME_ID__` is the folder name of the active game under src/games/ (GAME env var,
 * default 'swamp-funk'); the `@game/*` alias resolves to that folder.
 */
declare const __GAME_ID__: string;
