# TEMPLATE royal_material_pass.txt: optional AI surface pass on a VECTOR royal (L1-L5), in the formula D finish
# (docs/games/bass-drop/STYLE_DECISION.md; docs/ART_BIBLE.md section 2.1).
# Input: the resvg render of the vector glyph (art/source/ui/royal_<ID>.svg -> PNG on {KEY_HEX}) as the ONLY reference.
# Afterwards: re-matte (tools/matte/outline_matte.py --key auto: keys on the MEASURED background), then register the
# result back onto the vector alpha (OpenCV findTransformECC MOTION_AFFINE) so the silhouette, outline weight and
# spelling stay exactly the vector's.
# Placeholders: {GLYPH} {FACE_HEX} {FACE_NAME} {KEY_HEX}
# {KEY_HEX}: #00FF00, except L3 'Q' (green face) -> #FF00FF.
# Formula D: depth comes from the painted shading, never from a separate slab. Never write "extrusion" (the model
# draws a stick or a tab) or "molten" (it draws drips): say "polished".
Same letterform '{GLYPH}' as the reference: identical silhouette, outline thickness and framing; exactly one character, spelled exactly '{GLYPH}'. Change only the surface: a glossy {FACE_NAME} ({FACE_HEX}) lacquered enamel face set in a chunky carved cypress-wood rim, premium hand-painted rendering with smooth layered shading and soft gradients, strong key light from the upper left with a warm bounce light and a thin cool rim light, glossy lacquered highlights and crisp white specular hotspots, clear three-dimensional volume from the shading alone. Keep the clean single-weight bold black outline exactly as it is. Solid uniform flat {KEY_HEX} background, no other text, no cast shadow.
