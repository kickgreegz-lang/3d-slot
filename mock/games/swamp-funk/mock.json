{
  "$comment": "Mock RGS settings for this game (mock/rgsMockPlugin.ts). betModes: RGS mode key -> cost multiple and the book set it draws from (books_<pool>_*.json, then <pool>_fixtures.json).",
  "betModes": {
    "BASE": { "costMultiplier": 1, "pool": "base" },
    "BONUS": { "costMultiplier": 100, "pool": "bonus" }
  }
}
